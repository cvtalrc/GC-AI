# Ignorar el entorno virtual de Python
venv/
env/
*.venv
.virtualenv/

# Archivos de bytecode y caché de Python
_pycache_/
*.pyc
*.pyo
*.pyd

# Directorio .git y archivos de Git
.git/
.gitignore
.gitattributes

# Archivos y carpetas de configuración de IDEs
.idea/
.vscode/
*.sublime-project
*.sublime-workspace

# Archivos específicos del sistema operativo
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Archivos de configuración local o sensibles (si los tienes y no deben ir a la imagen)
.env
config.local.py
secrets/

# Archivos de dependencias de desarrollo (si tienes un archivo separado)
# requirements.dev.txt

# Artefactos de build/dist locales (si los generas fuera de Docker antes de construir)
# build/
# dist/
# *.egg-info/

# Bases de datos SQLite locales u otros archivos de bases de datos temporales
*.sqlite3
*.db

# Archivos de test y cobertura (si no los necesitas en la imagen final)
.pytest_cache/
htmlcov/
.coverage

# El propio Dockerfile y docker-compose.yml (no se deben copiar a sí mismos)
#Dockerfile
#docker-compose.yml 
# (Nota: si docker-compose.yml está en la raíz del proyecto general, no en la carpeta 'back', no necesitas la línea anterior aquí)

# Cualquier carpeta node_modules si accidentalmente existe en el backend
node_modules/