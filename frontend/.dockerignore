# Directorio de módulos de Node (estos se instalarán dentro de la imagen en la etapa de build)
node_modules/

# Logs de npm
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Archivos de configuración de IDEs y del sistema operativo
.idea/
.vscode/
*.sublime-project
*.sublime-workspace
.DS_Store
Thumbs.db

# Directorio .git y archivos de Git
.git/
.gitignore
.gitattributes

# Archivos de configuración local o sensibles
.env
.env.local
.env.*.local

# Carpeta de build local (si tu Dockerfile construye desde cero, no necesitas copiar una local)
# Si tu Dockerfile tiene RUN npm run build, esta carpeta dist o build
# se generará DENTRO del contenedor en la etapa de build, por lo que no necesitas
# ignorarla aquí explícitamente a menos que tengas una versión local que no quieras que interfiera.
# Sin embargo, es buena práctica ignorar node_modules del host.
# dist/
# build/

# El propio Dockerfile y docker-compose.yml
#Dockerfile
# docker-compose.yml # (Si está en la raíz del proyecto general)